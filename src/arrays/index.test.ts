import MyArray from "./index"

describe('Test cases for Array implementation', () => {
    test('insertion', () => {
        let arr = new MyArray();
        // arr.insert(1,0);
        // expect(arr.insert(2,1)).toThrow('Invalid location specified');
        // arr.insert(3,2);
        // arr.insert(4,3);
        // arr.insert(5,4);
        // expect(arr.getData(0)).toBe(1);
    })
})
